class Solution {
public:
    int maxProfit(vector<int>& prices) {
     int min1 = INT_MAX;
     int max1 = 0;
     for(auto x : prices){
        min1 = min(min1,x);
        max1 = max(max1,x-min1);
     }
     return max1;  
    }
};

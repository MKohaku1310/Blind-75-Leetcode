class Solution {
public:
    int missingNumber(vector<int>& nums) {
        int n = nums.size();
        long long sum = 1LL*n*(n+1)/2;
        long long sum2 = 0;
        for(int i=0;i<nums.size();i++){
            sum2+=nums[i];
        }
        return sum-sum2;
    }
};
